import 'package:flutter/material.dart';
import 'package:appcours/content/TableContent.dart';

class TablePage extends StatelessWidget {
  const TablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: TableContent()
    );
  }
}